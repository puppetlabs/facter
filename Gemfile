# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'thor', git: 'https://github.com/sebastian-miclea/thor.git'

# Specify your gem's dependencies in cli.rb-ng.gemspec
gemspec
