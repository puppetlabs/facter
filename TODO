* ffi wrapper
* Handle non-string values
* Normalize value and to_json to both return json?
* virtual
* cmake or autoconf?
* abstract out command execution method
* Add regex support (e.g. for os and processor file parsing)
* fact dependencies?
